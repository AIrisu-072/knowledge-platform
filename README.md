# Knowledge Platform

Knowledge Platformは、Document PlatformとSearch Platformを分離した仕様駆動の基盤です。

規範仕様は `spec/` にあります。正式な開発環境はLinux、macOS、Windows via WSL2です。Native Windowsは対象外です。

検証入口:

- `mise run verify:fast`
- `mise run verify`
- `mise run verify:full`
